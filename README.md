# Stundent-Management-System-JDBC

## 📌 Project Overview

This project is a simple Java JDBC-based application that demonstrates how to perform database operations using JDBC (Java Database Connectivity).

The application allows users to perform basic CRUD operations (Create, Read, Update, Delete) on a database.
